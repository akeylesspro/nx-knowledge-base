# Sync Agent Commands

- `pnpm run kb:verify-dispatch -- --payload .tmp/sync-payload.json`
- `pnpm run kb:generate-docs -- --payload .tmp/sync-payload.json --repo <repo_name>`
- `pnpm run kb:generate-openapi -- --payload .tmp/sync-payload.json --repo <repo_name>`
- `pnpm run kb:apply-overrides -- --repo <repo_name>`
- `pnpm run kb:build-search-index`
