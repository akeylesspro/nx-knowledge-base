# Sync Agent Style

- Keep generated JSON deterministic and stable.
- Preserve key ordering and formatting with 2-space indentation.
- Use `schema_version` and `doc_id` in every file doc.
- Include both NX-KB and GitHub deep links.
