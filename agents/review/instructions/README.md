# Review Agent Instructions

- Review PRs from Sync Agent before merge to `main`.
- Block merge when schema, links, tests, or security checks fail.
- Confirm manual overrides were not overwritten.
- Enable squash automerge only after all checks pass.
